# 金融数据分析与洗钱风险监测系统

## 项目概述

这是一个基于Python Flask的金融数据分析平台，专门用于银行交易数据的可视化分析和洗钱风险监测。系统通过分析交易记录，识别潜在的异常交易模式和洗钱风险指标，提供交易图谱可视化、发票图谱分析、预警管理等功能。

### 主要功能特性

- 📊 **交易数据可视化** - 交互式表格展示和D3.js力导向图
- 🔗 **交易关系图谱** - 展示账户间的资金往来关系
- 🧾 **发票图谱分析** - 分析销方和购方的发票往来
- ⚠️ **预警管理系统** - 添加/删除预警，实时高亮显示
- 🔍 **数据筛选功能** - 多维度筛选交易数据
- 📈 **统计分析** - 交易金额、频次等统计分析
- 🔄 **数据导入导出** - 支持Excel文件导入导出
- 🎯 **图谱简化** - 自动简化复杂图谱，突出核心节点

## 项目结构

```
新建文件夹/
├── hou/                          # 后端服务目录
│   ├── app.py                    # Flask主应用服务器
│   ├── simple.py                 # 图谱简化算法
│   ├── analyze_money_laundering.py  # 风险分析脚本
│   ├── change.py                 # 数据变更处理
│   ├── merge_data.py             # 数据合并工具
│   ├── note.py                   # 笔记记录功能
│   ├── read_excel.py             # Excel数据读取工具
│   ├── warning.py                # 预警处理模块
│   └── requirements.txt          # Python依赖包列表
├── qian/                         # 前端界面目录
│   ├── index.html                # 主数据展示页面
│   └── filter.html               # 数据筛选和图谱页面
├── temp/                         # 临时文件目录
├── .qoder/                       # Qoder配置目录
├── 建模数据121.xlsx              # 核心交易数据文件
├── 销项整理后.xlsx               # 发票数据文件
├── 预警.xlsx                     # 预警数据文件
└── 项目文档.md                   # 项目说明文档
```

## 核心功能实现

### 1. 数据读取与处理

#### 1.1 Excel数据读取 (`read_excel.py`)
```python
def read_excel_file(file_path):
    """
    读取Excel文件并返回数据
   支持多工作表读取，自动处理缺失值
    """
    try:
        excel_file = pd.ExcelFile(file_path)
        all_sheets_data = {}
        
        for sheet_name in excel_file.sheet_names:
            df = pd.read_excel(file_path, sheet_name=sheet_name)
            all_sheets_data[sheet_name] = df.fillna('').to_dict(orient='records')
        
        return all_sheets_data
    except Exception as e:
        print(f"读取Excel文件时出错: {e}")
        return None
```

#### 1.2 数据清洗预处理
-移除列名中的制表符(`\t`)
-统一数据格式标准化
-处缺失值和异常数据
- 时间格式统一转换

### 2.后端API服务 (`app.py`)

#### 2.1 Flask应用配置
```python
from flask import Flask, request, jsonify
from flask_cors import CORS
import pandas as pd

app = Flask(__name__)
CORS(app)  #允跨域请求
EXCEL_FILE_PATH = r'C:\Users\WYX15\Desktop\新建文件夹\建模数据121.xlsx'
```

#### 2.2核心API接口

**获取Excel数据接口**
```python
@app.route('/api/excel-data')
def get_excel_data():
    """
    读取Excel文件并返回JSON格式数据
    """
    try:
        df = pd.read_excel(EXCEL_FILE_PATH, sheet_name=0)
        data = df.fillna('').to_dict(orient='records')
        return jsonify({
            "data": data, 
            "total_rows": len(data), 
            "columns": df.columns.tolist()
        })
    except Exception as e:
        return jsonify({"error": str(e)}), 500
```

**数据存储接口**
```python
@app.route('/api/data', methods=['POST'])
def save_data():
    """
   接收前端发送的数据并存储
    """
    try:
        data = request.get_json()
        #验证必需字段
        if 'name' not in data or 'email' not in data:
            return jsonify({"error": "缺少必需字段: name 或 email"}), 400
        
        # 添加到数据存储
        new_entry = {
            "id": len(data_store) + 1,
            "name": data['name'],
            "email": data['email']
        }
        data_store.append(new_entry)
        
        return jsonify({
            "message": f"数据保存成功!欢 {data['name']}!",
            "saved_data": new_entry
        }), 200
    except Exception as e:
        return jsonify({"error": str(e)}), 500
```

### 3.风险分析引擎

#### 3.1大额交易检测
```python
#大额交易分析 (前5%)
large_threshold = df['交易金额'].quantile(0.95)  # 787万阈值
large_transactions = df[df['交易金额'] > large_threshold]
# 发现150笔大额交易
```

#### 3.2 高频交易识别
```python
#高频交易者检测
freq_threshold = df['交易方户名'].value_counts().quantile(0.95)  # 841笔阈值
frequent_traders = df['交易方户名'].value_counts()
high_freq = frequent_traders[frequent_traders > freq_threshold]
# 发现1个超高频交易者：山西*圣煤炭物流贸易有限公司 (978笔)
```

#### 3.3 时间异常分析
```python
# 交易时间分布分析
df['交易小时'] = df['交易时间'].dt.hour
hourly_dist = df['交易小时'].value_counts().sort_index()
#异常发现：0时-1时交易最活跃 (1235笔)
```

#### 3.4循交易模式检测
```python
#对双向交易检测
account_pairs = df_clean.groupby(['交易方户名', '对手户名']).size()
bidirectional = account_pairs[account_pairs > 1]
# 发现366个账户对存在多笔交易
```

### 4.前端可视化界面

#### 4.1 主数据展示页面 (`index.html`)

**核心功能模块：**
-📊 交互式数据表格展示
-🔍 实时数据筛选功能
- 📈 交易关系图谱可视化
- 📋详情查看
-⏱ 时间范围筛选

**关键技术实现：**

**D3.js关系图谱**
```javascript
// 创建力导向图
const simulation = d3.forceSimulation(nodes)
    .force('link', d3.forceLink(links).id(d => d.id).distance(100))
    .force('charge', d3.forceManyBody().strength(-300))
    .force('center', d3.forceCenter(width / 2, height / 2));

//颜色编码
function getNodeColor(group) {
    if (group === 'sender') return '#1f77b4';    // 交易方：蓝色
    if (group === 'receiver') return '#ff7f0e';  // 对手方：橙色
    return '#999';  //节点：灰色
}
```

**借贷标志可视化**
```javascript
//颜色编码
const linkColors = {
    '#d62728': '进: 交易方→对手方',  //：资金流入
    '#1f77b4': '出: 交易方→对手方',  //：资金流出
    '#7f7f7f': '出: 交易方→(无对手)', //：无对手账户
    '#bcbd22': '入: (无交易方)→对手方' //黄：无交易方
};
```

#### 4.2 数据筛选页面 (`filter.html`)

**筛选功能：**
-户精确搜索
-💳卡模糊匹配
-📊借标志筛选
-⏰范围选择
-💰 金额区间过滤

### 5.系统架构设计

#### 5.1技术栈
- **后端**: Python 3.12 + Flask 2.3.3
- **数据处理**: Pandas + NumPy
- **前端**: HTML5 + JavaScript + CSS3
- **可视化**: D3.js v7
- **通信**: RESTful API + JSON
- **跨域**: Flask-CORS 4.0.0

#### 5.2 数据流设计
```
Excel数据文件 → Pandas处理 → Flask API → JSON响应 →前端渲染
     ↓              ↓            ↓           ↓          ↓
  2990条记录    数据清洗分析    HTTP服务    JSON数据    D3可视化
```

#### 5.3安全考虑
- CORS跨域资源共享配置
- 数据验证和错误处理
- API接口参数校验
-异常情况优雅降级

## 部署与运行

### 🚀 快速启动（推荐）

**一键启动系统 - 双击 `start.bat` 文件即可！**

系统会自动完成以下步骤：
```
✅ STEP 1/3: 检查 Python 环境
✅ STEP 2/3: 自动检查和安装依赖包
✅ STEP 3/3: 启动 Flask 服务并打开浏览器
```

**执行流程：**
```bash
# 用户只需双击
start.bat

# 系统自动执行：
1. 检测 Python 版本（需要 3.8+）
2. 检查依赖包是否已安装
3. 如果缺少依赖，自动从 requirements.txt 安装
4. 升级 pip 到最新版本
5. 启动 Flask应用
6. 3 秒后自动打开浏览器访问 http://localhost:5000
```

**输出示例：**
```
==========================================
   金融数据分析与洗钱风险监测系统
==========================================

[STEP 1/3] 检查 Python 环境...
[OK] Python 已安装：Python 3.13.12

[STEP 2/3] 检查和安装依赖包...
[INFO] 发现 requirements.txt，开始检查依赖...
[OK] 所有依赖包已安装，无需重复安装

[STEP 3/3] 启动 Flask 后端服务...
[INFO] 服务地址：http://localhost:5000
[INFO] 浏览器将在 3 秒后自动打开...

Flask应用正在启动...
 * Running on http://127.0.0.1:5000
```

---

### 📋 手动部署（可选）

#### 1. 环境准备

**前提条件：**
- Python 3.8 或更高版本
- 下载地址：https://www.python.org/downloads/
- ⚠️ 安装时请勾选 "Add Python to PATH"

**安装依赖包：**
```bash
# 方法 1：使用 requirements.txt（推荐）
cd hou
pip install -r requirements.txt

# 方法 2：手动安装
pip install flask flask-cors pandas numpy openpyxl requests
```

**依赖包列表 (`hou/requirements.txt`)：**
```
# Web 框架
Flask==2.3.3
Flask-CORS==4.0.0
Werkzeug==2.3.3

# 数据处理
pandas>=1.5.0
numpy>=1.23.0

# Excel 支持
openpyxl>=3.0.0

# HTTP 请求（测试用）
requests>=2.28.0
```

---

#### 2. 启动服务

**方法 A：使用启动脚本（推荐）**
```bash
# Windows 系统
双击 start.bat
```

**方法 B：手动启动**
```bash
# 进入后端目录
cd hou

# 启动 Flask应用
python app.py

# 服务启动信息
Flask应用正在启动...
前端可以访问 http://localhost:5000
 * Running on http://127.0.0.1:5000
 * Running on http://192.168.101.6:5000
Press CTRL+C to quit
```

---

#### 3. 访问界面

- **主界面**: http://localhost:5000
- **API 测试**: http://localhost:5000/api/excel-data
- **健康检查**: http://localhost:5000/api/health
- **发票数据**: http://localhost:5000/api/invoice-data

## 数据分析结果

###统计
- **总交易笔数**: 2,990笔
- **总交易金额**: 60.5亿元人民币
- **平均交易金额**: 202.5万元
- **最大单笔交易**: 5,000万元
- **数据时间跨度**: 2015年

###指标发现

####🔴高风险信号
1. **大额交易集中**: 150笔交易超过787万阈值
2. **超高频交易**: 1个公司978笔交易 (山西*圣煤炭物流贸易有限公司)
3. **异常时间分布**:深0时交易最活跃 (1235笔)
4. **循环交易模式**: 366个账户对存在多笔交易

####注指标
-借标志基本平衡 (出:1551笔,进:1439笔)
-多个公司涉及大额交易活动
-间存在复杂交易网络关系

##扩功能建议

### 1.分析能力
- [ ] 机器学习异常检测算法
- [ ] 交易模式聚类分析
- [ ]评分模型
- [ ] 实时监控告警系统

### 2.界优化
- [ ]响应式移动端适配
- [ ]多维度数据钻取
- [ ] 自定义报表生成
- [ ] 数据导出功能

### 3.系统增强
- [ ] 数据库存储替代Excel
- [ ] 用户权限管理系统
- [ ]审日志记录
- [ ]多数据源支持

## 项目维护与支持

###常见问题解决

**1. 数据加载失败**
```bash
#检查文件路径
确认文件存在: C:\Users\WYX15\Desktop\新建文件夹\建模数据121.xlsx
# 检查权限
确保Python有文件读取权限
```

**2. API接口异常**
```bash
#测试API连通性
python hou/test_excel_api.py
#检查服务状态
curl http://localhost:5000/
```

**3.前端显示问题**
```bash
#清除浏览器缓存
Ctrl+F5强刷新
# 检查网络连接
确认localhost:5000服务正常运行
```

**4. Excel文件并发访问问题**
```bash
#问题现象: "File is not a zip file"错误
#解决方案:系统已添加文件锁和重试机制
# - 使用threading.Lock()防止并发冲突
# - 实现3次重试机制，每次间隔1秒
# - 添加详细的错误日志便于诊断
```

**5.户搜索功能问题**
```bash
#问题现象: filter.html中搜索不到特定户名
#解决方案: 修复页面加载事件执行顺序问题
# -合数据加载和URL参数处理逻辑
# -确保数据加载完成后再执行筛选
# -减自动筛选延迟时间
```

###性能优化建议
- 数据分页加载避免内存溢出
- 图谱节点数量限制 (建议<500个)
-异步数据处理提升响应速度
-缓机制减少重复计算

## 核心功能详解

### 1. 预警管理系统

#### 1.1 预警功能特性
- **实时预警** - 添加预警后自动高亮显示，无需刷新页面
- **多维度预警** - 支持金额阈值、频率异常、可疑模式等预警类型
- **风险分级** - 支持严重、高、中、低四个风险级别
- **自动同步** - 添加/删除预警后自动刷新筛选结果表格和图谱

#### 1.2 预警数据存储
- 使用浏览器 `localStorage` 存储预警数据
- 支持自动导出到Excel文件
- 预警数据持久化保存

#### 1.3 预警可视化
- **表格高亮** - 预警账户所在行显示红色背景和加粗字体
- **图谱标识** - 预警节点显示红色警告图标
- **实时更新** - 添加/删除预警后立即更新所有视图

### 2. 交易关系图谱

#### 2.1 图谱特性
- **力导向布局** - 使用D3.js力导向图自动布局
- **双向关系** - 支持显示交易的双向关系
- **颜色编码** - 根据借贷标志显示不同颜色
  - 🔴 红色：进（资金流入）
  - 🔵 蓝色：出（资金流出）
  - ⚪ 灰色：出到无对手
  - 🟡 黄色：从无交易方入

#### 2.2 图谱交互
- **节点拖拽** - 支持拖拽调整节点位置
- **点击查看详情** - 点击节点查看交易详情
- **全屏显示** - 支持全屏查看图谱
- **图谱简化** - 支持简化图谱，移除叶节点

#### 2.3 图谱简化算法
- **保护中心节点** - 简化时保护中心节点不被删除
- **迭代移除** - 迭代移除度数为1的叶节点
- **虚拟节点过滤** - 过滤掉单边交易创建的虚拟节点

### 3. 发票图谱分析

#### 3.1 发票数据展示
- **销方购方关系** - 展示销方和购方的发票往来关系
- **节点分类** - 销方显示红色，购方显示青色
- **汇总统计** - 节点详情显示发票总数、总金额、总税额

#### 3.2 发票详情
- 发票代码、发票号码
- 销方税号、购方税号
- 金额、税额、价税合计
- 开票日期

### 4. 数据筛选功能

#### 4.1 筛选条件
- **户名筛选** - 支持模糊搜索和通配符
- **卡号筛选** - 支持卡号精确和模糊匹配
- **借贷标志** - 筛选进/出账交易
- **金额范围** - 设置最小和最大金额
- **日期范围** - 选择开始和结束日期

#### 4.2 筛选结果
- **全量显示** - 显示所有匹配结果，不分页
- **实时筛选** - 应用筛选后立即显示结果
- **数据导出** - 支持导出筛选结果为Excel

### 5. 数据导入功能

#### 5.1 批量导入
- **拖拽上传** - 支持拖拽文件上传
- **自动识别** - 自动识别Excel文件格式
- **列名兼容** - 兼容带\t前缀和不带\t前缀的列名
- **自动刷新** - 导入后自动刷新数据和图谱

#### 5.2 数据追加
- **追加模式** - 支持向现有数据追加新数据
- **去重处理** - 自动处理重复数据
- **实时更新** - 追加后立即更新显示

## 本次更新内容 (2026年3月)

###🔧界面优化

**1. 预警数据字体颜色优化**

- **问题**: 在filter.html和index.html中，预警账户数据没有明显的视觉标识
- **解决方案**: 为包含预警的交易数据添加红色字体标识
- **实现细节**:
  - 在filter.html的表格渲染中，添加对预警账户的检查逻辑
  - 使用`hasAlertForNode()`函数判断账户是否有关联预警
  - 为有预警的行添加`.alert-row` CSS类和红色字体样式
  - 在index.html中同样实现预警数据的红色字体显示
- **影响文件**: `qian/filter.html`, `qian/index.html`

**2. 预警节点图标替换**

- **问题**: 图谱中的预警节点使用普通圆形图标，无法直观识别风险节点
- **解决方案**: 为有预警的节点替换为醒目的警告图标
- **实现细节**:
  - 在filter.html的D3.js图谱中，为每个节点检查预警状态
  - 有预警的节点使用红色警告SVG图标 (`<svg>`元素)
  - 无预警的节点保持原有的圆形样式
  - 图标缩放比例设置为0.04，比原来放大一倍
- **影响文件**: `qian/filter.html`

###🗑️ 文件整理

###🔧算法增强

**3. 图谱简化算法增强**

- **问题**: 原有的图谱简化算法只能移除叶节点（度数为1的节点），无法移除孤立节点（度数为0的节点）
- **解决方案**: 增强图谱简化算法，使其能够同时移除孤立节点和叶节点
- **实现细节**:
  - 在`hou/simple.py`中新增`remove_isolated_and_leaf_nodes()`函数
  - 修改`remove_leaf_nodes_from_source()`函数，使其也移除度数为0的孤立节点
  - 更新`build_full_transaction_topology()`函数，支持新的简化选项
  - 修改后端API `/api/simplify-graph`，支持移除孤立节点功能
  - 前端在调用简化功能时默认启用移除孤立节点和叶节点
- **影响文件**: `hou/simple.py`, `hou/app.py`, `qian/filter.html`

**1. 清理测试和临时文件**

- **问题**: 项目中存在多个测试文件和临时文件，影响项目整洁度
- **解决方案**: 删除所有测试和临时文件
- **删除的文件**:
  - `add_test_alerts.js`
  - `complete_test.html`
  - `demo_alerts.html`
  - `test_alerts.html`
  - `test_export.py`
  - `test_export_api.py`
  - `test_history.html`
  - `hou/test_api.py`
  - `hou/test_excel_api.py`
  - `qian/test_alerts.json`
  - `~$预警.xlsx` (临时文件)
- **保留的核心文件**:
  - 核心源代码 (Python、HTML文件)
  - 数据文件 (Excel文件)
  - 配置文件 (requirements.txt)
  - 项目文档

### 📊系统增强

**1. 用户体验提升**
- 预警信息可视化增强，通过颜色和图标直观显示风险
- 图表中风险节点更容易识别
- 数据表格中的预警账户突出显示

**2. 代码质量提升** 
- 统一的预警处理逻辑
- 清理后的项目结构更加清晰
- 减少了不必要的测试文件干扰

###✅ 功能验证

所有新增功能均已通过测试验证:
- 预警账户数据正确显示红色字体
- 预警节点正确显示警告图标
- 系统运行稳定
- 数据完整性保持完好

---

## API接口文档

### 后端API列表

| 接口路径 | 方法 | 功能说明 |
|---------|------|---------|
| `/` | GET | 健康检查 |
| `/api/excel-data` | GET | 获取交易数据 |
| `/api/invoice-data` | GET | 获取发票数据 |
| `/api/data` | POST | 保存数据 |
| `/api/import-excel` | POST | 导入Excel文件 |
| `/api/simplify-graph` | POST | 简化图谱 |
| `/api/export-alerts` | POST | 导出预警数据 |
| `/api/build-topology` | POST | 构建交易拓扑 |
| `/api/invoice-graph` | POST | 获取发票图谱数据 |

---

## 浏览器兼容性

- ✅ Chrome 90+
- ✅ Firefox 88+
- ✅ Edge 90+
- ✅ Safari 14+

---

**项目状态**: ✅ 运行中  
**最后更新**: 2026年3月  
**数据源**: 建模数据121.xlsx、销项整理后.xlsx  
**服务地址**: http://localhost:5000  
**当前版本**: v2.0 (包含完整预警系统、图谱简化和发票分析)